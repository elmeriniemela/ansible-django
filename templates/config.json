{
    "POSTGRESQL": {
        "USER": "{{ websites_user }}",
        "PASSWORD": "{{ website_db_password }}"
    },
    "SECRET_KEY": "{{ website_secret_key }}",
    "ALLOWED_HOSTS": [
        "www.thecodebase.tech"
    ],
    "LOG_FILE": "{{ websites_home_dir }}/log/django-application.log"
}